---
context: phase
phase: 7-main-orchestrator
status: not_started
last_updated: 2026-05-29T15:47:22Z
---

<current_state>
Phases 1–6 are complete and validated. The full pipeline ran end-to-end on Sven (UX Design Lead) producing 30 deployment artifacts across 7 targets for ~$4.00 in ~30 min. All planning documents (ROADMAP.md, ARCHITECTURE.md, PROJECT.md) are updated. Working tree is clean — no uncommitted source changes.

Phase 7 (Main Orchestrator) is the final phase and has not been started.
</current_state>

<completed_work>

This session:
- Phase 6 implementation: target_deploy.py (4-phase pipeline), 7 prompt files, CLI wiring, LiteLLM config
- Full pipeline validation: Sven ran Stages 1–6 end-to-end successfully
- Bug fix: EARL Pass 5 audit switched from general tier (128K limit) to reasoning tier (Sven hit 157K tokens)
- Planning doc updates: ROADMAP, ARCHITECTURE, PROJECT all reflect Phase 6 completion

All phases complete:
- Phase 1: DEA Context Creation — complete
- Phase 2: Expert Six Prompt Creation — complete
- Phase 3: Expert Six Execution and Finalization — complete
- Phase 4: Epistemic Anchor Creation — complete
- Phase 5: EARL Agent Package Creation — complete
- Phase 6: Target Deployment Packaging — complete
</completed_work>

<remaining_work>

Phase 7: Main Orchestrator
- `dea-builder run <workspace>` — full pipeline end-to-end
- `dea-builder plan <workspace>` — planning/dry-run mode
- Stage dependency validation (each stage checks upstream output exists)
- Skip completed stages (output already valid)
- Forced re-run (`--force-stage <N>`)
- Top-level pipeline trace linking all stage traces
- Cost summary across all stages
- `--skip-package` and `--targets` pass-through to Stage 6
</remaining_work>

<decisions_made>

- Pass 5 audit uses reasoning tier (not general) to handle large anchors (286bcb4)
- 7 target platforms: PAS (always-on) + ChatGPT Custom GPT (copy) + 5 LLM-generated (6e5499a)
- Hermes and Grok fully independent — Hermes is runtime framework, Grok is model-bound (261dd6d)
- Knowledge manifest (names/descriptions only) as pipe input, not full content — cost optimization
- adl_version stays at 1.0
- Fidelity inspection catches semantic drift: Hermes FAIL (invented memory), others WARN
</decisions_made>

<blockers>
None. All infrastructure, credentials, and dependencies are in place.
</blockers>

## Required Reading (in order)
1. `.planning/ARCHITECTURE.md` — full architecture including Stage 6 implementation details
2. `.planning/ROADMAP.md` — Phase 7 requirements
3. `src/dea_builder/cli/main.py` — existing CLI dispatch pattern (all 6 stage commands work)
4. `src/dea_builder/stages/target_deploy.py` — Stage 6 as reference for pipeline pattern

## Infrastructure State
- Azure OpenAI: `aoai-gpt55-trusted` deployment active, Prompt Shields OFF for Modules 2–6
- Content Safety: `aidc-content-safety` for Module 1 trust boundary
- Key Vault: `kv-aidc-eus2` — all secrets in place
- LiteLLM: config at `litellm.config.yaml`, startup script at `scripts/start-litellm.sh`
- Workspace outputs: `workspaces/_test_stage1/` has full Sven pipeline output (Stages 1–6)

<context>
The project is at 6/7 phases (86%). All individual stage commands work. Phase 7 wires them into a single orchestrated `run` command. The existing CLI pattern in main.py is straightforward — each stage has a `_run_<stage>` function. The orchestrator needs to call them in sequence with dependency checking and skip logic. This should be the simplest phase since all the hard work (LLM pipelines, prompts, content safety, target packaging) is done.
</context>

<next_action>
Start with: Design and implement Phase 7 — `dea-builder run <workspace>` orchestrator that calls Stages 1–6 in sequence with dependency validation, skip logic, `--force-stage`, `--skip-package`, `--targets` pass-through, and aggregated cost/trace reporting.
</next_action>
